%nav.navbar.navbar-expand-lg.navbar-dark.bg-dark
  = link_to "ZurNow", "/", class: "navbar-brand"
  %ul.navbar-nav.mr-auto
    - if user_signed_in?
      = link_to 'Lista organizacji', organizations_path, class: "nav-link"
  %ul.navbar-nav.ml-auto
    - if user_signed_in?
      %li.nav-item
        = link_to edit_user_path(current_user), class: "nav-link" do
          = current_user.email
      %li.nav-item
        = link_to "Wyloguj", destroy_user_session_path, class: "nav-link", method: :delete
    - else
      %li.nav-item= link_to 'Zarejestruj się', new_user_registration_path, class: "nav-link"
      %li.nav-item= link_to 'Logowanie', user_session_path, class: "nav-link"
